# build output
bin/
obj/

# run output (regenerated by `extract`)
graph.json
frontier.json
lineage.json
# extract/demo scratch outputs (e.g. step1-graph.json from the adventureworks demo)
*-graph.json
*-frontier.json

# viz output (regenerated by `viz`)
graph.html
*.html

# scratch / stage dirs from the spider loop
stage/
_stage/
_sample/

# adventureworks demo artifacts (left for inspection)
_demo-out/

# local validation against a real database (contains real vendor DDL — never publish)
_tmwtest/
